import { NbdProductTypeFilterPipe } from './nbd-product-type-filter.pipe';

describe('NbdProductTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NbdProductTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
